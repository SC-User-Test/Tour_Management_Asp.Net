@page "{id:int}"
@model TourManagement.Web.Pages.Tours.DetailsModel
@{
    ViewData["Title"] = "Tour Details";
}

<h1>Tour Details</h1>

@if (Model.Tour == null)
{
    <div class="alert alert-warning">Tour not found.</div>
}
else
{
    <div class="card">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Tour Name</dt>
                <dd class="col-sm-9">@Model.Tour.TourName</dd>

                <dt class="col-sm-3">Place</dt>
                <dd class="col-sm-9">@Model.Tour.Place</dd>

                <dt class="col-sm-3">Days</dt>
                <dd class="col-sm-9">@Model.Tour.Days</dd>

                <dt class="col-sm-3">Price</dt>
                <dd class="col-sm-9">@Model.Tour.Price.ToString("C")</dd>

                <dt class="col-sm-3">Locations</dt>
                <dd class="col-sm-9">@Model.Tour.Locations</dd>

                <dt class="col-sm-3">Tour Info</dt>
                <dd class="col-sm-9">@Model.Tour.TourInfo</dd>

                @if (!string.IsNullOrEmpty(Model.Tour.PictureFileName))
                {
                    <dt class="col-sm-3">Picture</dt>
                    <dd class="col-sm-9">
                        <img src="~/uploads/@Model.Tour.PictureFileName" alt="@Model.Tour.TourName" class="img-thumbnail" style="max-width: 400px;" />
                    </dd>
                }

                <dt class="col-sm-3">Created Date</dt>
                <dd class="col-sm-9">@Model.Tour.CreatedDate.ToString("g")</dd>

                @if (Model.Tour.ModifiedDate.HasValue)
                {
                    <dt class="col-sm-3">Modified Date</dt>
                    <dd class="col-sm-9">@Model.Tour.ModifiedDate.Value.ToString("g")</dd>
                }

                <dt class="col-sm-3">Active</dt>
                <dd class="col-sm-9">@(Model.Tour.IsActive ? "Yes" : "No")</dd>
            </dl>
        </div>
    </div>

    <div class="mt-3">
        <a asp-page="Edit" asp-route-id="@Model.Tour.Id" class="btn btn-warning">Edit</a>
        <a asp-page="Index" class="btn btn-secondary">Back to List</a>
    </div>
}
